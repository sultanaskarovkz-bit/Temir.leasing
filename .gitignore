# Исходные PDF от заказчика лежат в корне, но в репозиторий их не кладём.
# Чистые копии каталогов и сертификата дублируются в assets/ и публикуются оттуда.
# Прайс-лист с ценами НЕ публикуем (на сайте цен нет).
/*.pdf

# Системные файлы
Thumbs.db
.DS_Store
desktop.ini
